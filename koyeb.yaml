name: ArslanMD Official
services:
  - name: ARSLAN-MD-SERVICE
    type: web
    ports:
      - 5000
    routes:
      - path: "/"
    build:
      type: docker
      dockerfile: ./Dockerfile
    regions:
      - fra
      - sin
      
